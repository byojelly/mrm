<h1>Category: <b><%=@category.name.capitalize%></b><br></h1>
Items in this category:
<% @category.items.each do |item| %>
<li><%= link_to item.name, shop_item_path(item.shop, item)%> on sale in <%=item.shop.user.contact.city%>, <%=item.shop.user.contact.state%></li>
<%end%>
