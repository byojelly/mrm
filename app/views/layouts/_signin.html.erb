<nav class="signin">
  <%#binding.pry%>
        <%if current_user || session[:user_id]%>
              <% if current_user%>
                    You are signed in as <%=current_user.email%> |
                    <%=link_to "Sign Out", destroy_user_session_path, :method => "delete"%><br>
                    <%#binding.pry%>
                    <h1>Edit your contact information code this later</h1><%#=link_to 'here', edit_contact_path(@user.category.build)%>
              <%else%>
                     You are signed in as <%@user.email%>
                     <h1>Edit your contact information code this later</h1> <%#=link_to 'here', edit_contact_path(@user.category.build)%>
              <% end%>
        <%elsif current_page?('/users/sign_in')%>

        <%else%>
              Sign in <%= link_to "here", new_user_session_path%>
        <%end%><br>
</nav>
